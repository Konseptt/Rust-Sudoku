[package]
name = "suko_solver"
version = "0.1.0"
edition = "2018"

[lib]
name = "suko_solver"
path = "src/lib.rs"

[dependencies]
rand = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
strip = true